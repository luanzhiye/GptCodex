<Window x:Class="WheelSpeedWPF.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="关于 Wheel Speed"
        Width="360"
        Height="240"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FFFDFEFE">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Wheel Speed"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,12" />

        <TextBlock Grid.Row="1"
                   Text="车轮旋转视觉效果模拟"
                   FontSize="14"
                   Foreground="#475569" />

        <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="8" Padding="12" Margin="0,16,0,16">
            <TextBlock TextWrapping="Wrap">
                本程序展示了车轮在不同转速与采样频率下的视觉效果，支持调整辐条数量、标记辐条以及控制刷新频率。
            </TextBlock>
        </Border>

        <Button Grid.Row="3" Content="确定" Width="80" Height="32" HorizontalAlignment="Right" Click="CloseButton_OnClick" />
    </Grid>
</Window>